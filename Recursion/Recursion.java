package Recursion;
void openBox(currentBox, 循环次数i) {
    if(i >= 20){
        return finish iterate
    }
    openBox;
    if(key in the box) {
        return key;
    }
    else {
        i ++;
        openBox(nextBox, i)
    }
}